# Product account integration

The canonical operator guide is [SDK configuration](../../packages/sdk/CONFIGURATION.md). It ships inside the SDK distribution, so product integrators can configure identity providers without reading server implementation files.

For the client API, see the [SDK README](../../packages/sdk/README.md).
